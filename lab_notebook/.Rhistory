setwd("~/current_research/CPOM_Flux/lab_notebook")
loi <- read.table("./data/leached_litter_LOI_5nov2015.csv", header = T, sep = ",")
mass.loss <- loi$AFDM.leaf[loi$treat != "PRE"] - (mean(loi$AFDM.leaf[loi$AFDM.leaf == "PRE"]))
mean(loi$AFDM.leaf[loi$AFDM.leaf == "PRE"])
mean(loi$AFDM.leaf[loi$AFDM.leaf == "PRE"], na.rm = T)
mass.loss <- loi$AFDM.leaf[loi$treat != "PRE"] - (mean(loi$AFDM.leaf[loi$treat == "PRE"]))
mass.loss <- (mean(loi$AFDM.leaf[loi$treat == "PRE"]) - loi$AFDM.leaf[loi$treat != "PRE"])
tapply(mass.loss, loi$treat, summary)
tapply(mass.loss, loi$treat[loi$treat != "PRE"], summary)
plot(mass.loss ~ jitter(as.numeric(treat), 1), data = loi, subset = sample == "leaf")
plot(mass.loss ~ treat, data = loi, subset = sample == "leaf")
plot(mass.loss ~ treat, data = loi)
plot(mass.loss ~ treat, data = loi, subset = treat != "PRE")
inital.leaf.mean <- (mean(loi$AFDM.leaf[loi$treat == "PRE"])
)
inital.leaf.mean <- (mean(loi$AFDM.leaf[loi$treat == "PRE"]))
mass.loss <- initial.leaf.mass - loi$AFDM.leaf[loi$sample == "leaf"])
mass.loss <- initial.leaf.mass - loi$AFDM.leaf[loi$sample == "leaf"]
initial.leaf.mean <- (mean(loi$AFDM.leaf[loi$treat == "PRE"]))
mass.loss <- initial.leaf.mass - loi$AFDM.leaf[loi$sample == "leaf"]
initial.leaf.mean <- (mean(loi$AFDM.leaf[loi$treat == "PRE"]))
mass.loss <- initial.leaf.mass - loi$AFDM.leaf[loi$sample == "leaf"]
mass.loss <- initial.leaf.mean - loi$AFDM.leaf[loi$sample == "leaf"]
tapply(mass.loss, loi$treat[loi$sample != "leaf"], summary)
tapply(mass.loss, loi$treat[loi$sample == "leaf" & != "PRE"], summary)
tapply(mass.loss, loi$treat[loi$sample == "leaf"], summary)
plot(mass.loss ~ treat, data = lo1)
plot(mass.loss ~ treat, data = loi)
plot(mass.loss ~ treat, data = loi, subset = treat != "PRE")
length(loi$treat[loi$sample == "leaf"])
length(mass.loss)
plot(mass.loss ~ treat, data = loi, subset = treat != "leaf")
plot(mass.loss ~ treat, data = loi, subset = treat == "leaf")
plot(mass.loss ~ treat, data = loi, subset = sample == "leaf")
plot(loi$treat[loi$sample == "leaf"), mass.loss)
plot(loi$treat[loi$sample == "leaf"], mass.loss)
treatment <- loi$treat[loi$sample == "leaf"]
treatment <- loi$treat[loi$sample == "leaf" & loi$treat != "PRE"]
plot(mass.loss ~ treatment)
length(treatment)
treatment <- loi$treat[loi$sample == "leaf"]
plot(mass.loss ~ treatment)
plot(mass.loss ~ treatment, data = loi, subset = treat != "PRE")
mass.loss <- mass.loss[loi$treat != "PRE"]
tapply(mass.loss, loi$treat[loi$sample == "leaf"], summary)
tapply(mass.loss, loi$treat[loi$sample == "leaf" & loi$treat != "PRE"], summary)
mass.loss <- initial.leaf.mean - loi$AFDM.leaf[loi$sample == "leaf"]
mass.loss <- mass.loss[8,]
mass.loss <- mass.loss[,8]
mass.loss <- mass.loss[1,8]
mass.loss <- mass.loss[8,1]
mass.loss[loi$treat != "PRE"]
mass.loss[loi$treat != "PRE" & loi$sample == "leaf"]
mass.loss
plot(mass.loss ~ treatment, data = loi, subset = treat != "PRE")
plot(mass.loss ~ treatment, data = loi, subset = treat != "PRE")
mass.loss[, 8]
mass.loss[8]
mass.loss[1:8]
mass.loss[1:8]
mass.loss <- initial.leaf.mean - loi$AFDM.leaf[loi$sample == "leaf"]
#remove the PRE samples
mass.loss[1:8]
mass.loss <- mass.loss[1:8]
tapply(mass.loss, loi$treat[loi$sample == "leaf" & loi$treat != "PRE"], summary)
treatment <- loi$treat[loi$sample == "leaf" & loi$treat != "PRE"]
treatment <- as.numeric(treatment)
plot(mass.loss ~ treatment)
anova(lm(mass.loss ~ as.factor(treatment))
)
