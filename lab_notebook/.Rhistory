loi <- read.table("./data/leached_litter_LOI_5nov2015.csv", header = T, sep = ",")
setwd("~/current_research/CPOM_Flux/lab_notebook")
d
loi <- read.table("./data/leached_litter_LOI_5nov2015.csv", header = T, sep = ",")
loi
tapply(loi$propOM[loi$sample == "sed"], loi$treat[loi$sample == "sed"], summary)
tapply(loi$propOM[loi$sample == "sed"], loi$treat[loi$sample == "sed"], stem)
kruskal.test(propOM ~ treat, data = loi, subset = sample == "sed")
loi.init <- read.table("./data/leached_litter_initial_LOI_17Sept2015.csv", header = T, sep = ",")
kruskal.test(loi$propOM[loi$sample == "sed" & loi$treat == "S"] ~ loi.init$propOM[loi.init$sample == "sed"])
kruskal.test(loi$propOM[loi$sample == "sed" & loi$treat == "LS"] ~ loi.init$propOM[loi.init$sample == "sed"])
kruskal.test(loi$propOM[loi$sample == "sed" & loi$treat == "S"], loi.init$propOM[loi.init$sample == "sed"])
kruskal.test(loi$propOM[loi$sample == "sed" & loi$treat == "LS"], loi.init$propOM[loi.init$sample == "sed"])
wilcox.test(loi$propOM[loi$sample == "sed" & loi$treat == "S"], loi.init$propOM[loi.init$sample == "sed"])
wilcox.test(loi$propOM[loi$sample == "sed" & loi$treat == "LS"], loi.init$propOM[loi.init$sample == "sed"])
wicox.test(propOM ~ treat, data = loi, subset = sample == "sed")
wilcox.test(propOM ~ treat, data = loi, subset = sample == "sed")
t.test(propOM ~ treat, data = loi, subset = sample == "sed")
t.test(loi$propOM[loi$sample == "sed" & loi$treat == "LS"], loi.init$propOM[loi.init$sample == "sed"])
t.test(loi$propOM[loi$sample == "sed" & loi$treat == "S"], loi.init$propOM[loi.init$sample == "sed"])
wilcox.test(loi$propOM[loi$sample == "sed" & loi$treat == "S"], loi.init$propOM[loi.init$sample == "sed"])
tapply((100 - perc.mass.rem), loi$treat[loi$sample == "leaf" & loi$treat != "PRE"], summary)
initial.leaf.mean <- (mean(loi$AFDM.leaf[loi$treat == "PRE"]))
mass.loss <- initial.leaf.mean - loi$AFDM.leaf[loi$sample == "leaf"]
perc.mass.rem <- (1 - (mass.loss / initial.leaf.mean)) * 100
#remove the PRE samples
mass.loss <- mass.loss[1:8]
perc.mass.rem <- perc.mass.rem[1:8]
tapply((100 - perc.mass.rem), loi$treat[loi$sample == "leaf" & loi$treat != "PRE"], summary)
tapply((100 - perc.mass.rem), loi$treat[loi$sample == "leaf" & loi$treat != "PRE"], sd)
